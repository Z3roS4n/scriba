# Decisioni di design

> Log delle decisioni prese durante il redesign/nuovo design UI — scope, processo, scelte tecniche.
> Formato breve: una voce per decisione, con data e motivazione. Per i gap funzionali (design →
> nessun backend) vedi [da-implementare.md](da-implementare.md); per lo stile vedi
> [design-system.md](design-system.md).

## D-UI-01 — Scope (compilare)
<Quali modalità/piattaforme si implementano ora, cosa è rinviato. Vedi `design-system.md#scope-attuale`.>

## D-UI-02 — Processo (compilare se rilevante)
<Se il lavoro è distribuito su più modelli/agenti (es. uno progetta, un altro implementa fedelmente
ogni sezione, un altro registra i gap), scriverlo qui — utile a chi riprende il lavoro dopo.>

## D-UI-03 — Verifica visiva senza login (se applicabile)
<Se le schermate sono autenticate: metodo scelto (harness di anteprima dev-only), cadenza di
consegna/review concordata con il cliente/PM.>

<!--
Template voce successiva:

## D-UI-0X — <titolo breve> ({{TODAY}})
<Cosa si è deciso e perché — 2-4 righe, senza rifare tutto il ragionamento.>
-->
