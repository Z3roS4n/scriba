# research/ — Note di ricerca

Una nota per ogni indagine tecnica (API di terzi, valutazioni, spike). Naming:

```
{YYYY-MM-DD}.{slug-argomento}.md
```

Esempio: `2026-07-08.api-terze-parti.md`

Genera con `/research <argomento>` oppure:
```
node .claude/scripts/new-research.mjs "argomento"
```
