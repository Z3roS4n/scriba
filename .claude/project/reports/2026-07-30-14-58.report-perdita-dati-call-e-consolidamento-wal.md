# Report — Perdita dei dati di una call: causa, recupero, e le tre correzioni

**Data:** 2026-07-30 14:58
**Decisione registrata:** `D-009` (02-tradeoffs.md), tre voci nuove in `07-bad-practices.md`

## Cos'era successo

L'analisi di una call di due ore («Call con Andrea», 30/07, 118 minuti) non partiva, e nel
database quella call non c'era più. Due guasti distinti, scoperti uno dietro l'altro.

### 1. L'analisi: sessione OAuth scaduta, e un errore illeggibile

`claude auth status` → `loggedIn: false`. La CLI usciva con codice 1 scrivendo comunque la
sua busta JSON, dove `result` diceva *«Failed to authenticate: OAuth session expired and
could not be refreshed»* — ma il nostro messaggio mostrava i primi 300 caratteri del JSON
grezzo, e la frase utile cadeva appena oltre il taglio. In più `available()` guardava solo
il PATH, quindi l'abbonamento risultava disponibile e l'analisi partiva per morire subito.

### 2. Il database: una giornata di lavoro dentro un WAL che nessuno consolidava

Ricostruzione dai file, non da ipotesi:

- `scriba.sqlite` fermo al **29/07 15:28**, 66 pagine. `scriba.sqlite-wal`: 4,2 MB, 1014
  frame, 5 generazioni, e i commit dichiaravano un database da **108 pagine**.
- 18 pagine non esistevano né nel file principale né in nessuna generazione del WAL →
  `PRAGMA integrity_check` → «database disk image is malformed».
- Nel codice: **nessun** `wal_checkpoint`, e `Store.close()` mai chiamato dal server.
- `Sidecar.stop()` → `child.kill()`; il core → `os._exit(0)` sul guinzaglio dello stdin.
  Entrambe le strade saltano l'unico momento in cui SQLite avrebbe consolidato.
- In sviluppo il core vero è un **nipote** di Electron: il `python.exe` del venv creato con
  uv ri-esegue l'interprete di base (verificato: `ParentProcessId` 17268 → 13884 → 31288).
  `kill()` su Windows uccideva lo stub e lasciava il core vivo, col database aperto. Al
  build successivo ne partiva un altro sullo stesso file — è lo stesso guasto che il commit
  `d601f80` descriveva («quattro copie dell'app aperte insieme»).

**Quello che resta ignoto:** perché il file principale fosse tornato a 66 pagine con la
data del 29/07 non lo so con certezza. Le generazioni del WAL dicono che dei checkpoint
sono avvenuti, e un checkpoint quel file l'avrebbe fatto crescere. Compatibili: un file
sostituito da una copia con data preservata, o un core orfano che continuava a scrivere sul
file originale mentre a quel nome era comparsa una copia. Non ci sono prove per scegliere.
L'esposizione — una giornata di dati in un WAL non consolidato, e un core che sopravvive al
padre — è invece dimostrata, e le correzioni valgono in entrambi i casi.

## Il recupero della call

Niente di sostanziale era perso, ma non grazie all'applicazione: grazie a un export fatto a
mano. Cosa c'era e dove:

| | dove | esito |
|---|---|---|
| Audio (118 min) | `audio/sessione-00002/{mic,loopback}.wav`, 227 MB ciascuno | intatto |
| Trascrizione | export Markdown 153 KB, 1745 righe | intatta |
| Riassunto + punti salienti | dentro il WAL | recuperati (3.629 e 2.472 caratteri) |
| Segmenti nel database | — | persi |
| «Riunione con Marco» 28/07 | file principale | intatta (363 segmenti, 15 task) |

Ricostruzione: base il file principale integro, sopra la call di oggi con id **4** (il 2 era
già di un'altra sessione: riusarlo per far tornare un numero avrebbe perso quella per
salvare questa). Trascrizione riletta dall'export — 774 segmenti con i minuti, 19 schermate
— più la riga di sessione e gli output dell'analisi dal WAL. Esito: `integrity_check: ok`,
nessuna violazione di chiave esterna, 774 segmenti su 774 nell'indice di ricerca. Le task
non c'erano in nessuna fonte: si rifanno rilanciando l'analisi, che ora ha i segmenti su cui
appoggiare le prove.

I file danneggiati **non** sono stati cancellati: `data/danneggiato-2026-07-30/`, più due
cartelle di backup (`backup-2026-07-30-grezzo` è l'unica copia intatta del WAL originale).

## Le correzioni

| File | Cosa |
|---|---|
| `core/scriba_core/db/store.py` | `consolida()` — `wal_checkpoint(TRUNCATE)` con ritentativi |
| `core/scriba_core/db/manutenzione.py` | nuovo: `controlla()` (quick_check → quarantena → ripristino), `backup()` con `VACUUM INTO` e rotazione di 5 |
| `core/scriba_core/server.py` | controllo prima di aprire il database; backup all'avvio e a fine registrazione; consolidamento a fine registrazione, a fine analisi e allo spegnimento; `_exit_when_orphaned` chiede a uvicorn di fermarsi e solo dopo 4 s esce di forza; `/health` riporta `db_danneggiato` |
| `ui/main/sidecar.ts` | `stop()` attende che il core esca da solo, poi uccide **l'albero** (`taskkill /T`) |
| `ui/main/index.ts` | `will-quit` trattiene la chiusura finché il core non ha consolidato; lucchetto di istanza singola |
| `core/scriba_core/llm/claude_cli.py` | l'errore riporta `result` invece della busta JSON; riconosce il guasto di accesso e dice `claude auth login`; `available()` interroga `claude auth status` |
| `core/tests/test_manutenzione_db.py` | nuovo, 14 test |
| `core/tests/test_claude_cli.py` | +6 test sull'accesso scaduto |

## Verifiche

- `test_manutenzione_db.py` → 14 passati. Il test che conta apre una copia del **solo**
  `.sqlite`, senza WAL, e chiede se la call è là dentro: senza `consolida()` non c'è nemmeno
  lo schema, dopo ci sono tutti i segmenti.
- `test_claude_cli.py` → 26 passati (20 prima).
- **Suite completa del core → 356 passati, nessuno fallito** (336 la baseline di `main`:
  +20 fra manutenzione e provider). Eseguita con `core/.venv` creato a fine giornata —
  Python 3.12.13 preso dall'interprete gestito che era rimasto nell'altra copia del repo,
  copiato in `.tools/python/` per non lasciare il venv agganciato a una cartella che verrà
  cancellata. Le esecuzioni precedenti giravano su un ambiente temporaneo senza
  numpy/comtypes/pycaw e dovevano escludere otto file di test, fra cui
  `test_automatismi.py`, che è quello che copre `/session/stop`.
- `npm run typecheck` → pulito.
- Database ricostruito installato e verificato in `%APPDATA%\scriba-ui\data`:
  `integrity_check: ok`, 4 sessioni, nessun WAL residuo.

## Rinviato

- **Il percorso Electron non è provato in esecuzione**: `stop()` con l'attesa e il
  `taskkill /T`, e il lucchetto di istanza singola, sono verificati solo dal typecheck.
  Vanno guardati al primo avvio vero — chiudendo l'app e controllando che nessun `python.exe`
  sopravviva e che il `-wal` risulti azzerato.
- **`/health` riporta `db_danneggiato` ma l'interfaccia non lo mostra ancora.** Se un
  database viene messo da parte, oggi lo dicono solo i log. Va scritto in `da-implementare.md`
  come voce P1: è esattamente il momento in cui l'utente deve sapere cos'è successo.
- Ri-trascrivere un audio salvato non esiste come funzione. Sarebbe la rete definitiva (i
  WAV sopravvivono a qualunque guaio del database), ed è il modo per ricostruire una call
  senza dipendere da un export fatto a mano.
- L'ambiente Python di questa copia del repo è stato creato a fine giornata **senza `uv`**,
  che sulla macchina non risulta più installato: l'interprete 3.12.13 gestito era rimasto
  in `aitools/scriba/.tools/`, è stato copiato in `.tools/python/` e il venv creato con
  `python -m venv`. Il README continua a documentare `uv`, che resta la strada consigliata
  per chi parte da zero.
