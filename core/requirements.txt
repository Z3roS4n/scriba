# Dipendenze del core di Scriba.
#
# Solo quelle dirette: il resto arriva da sé. Le versioni sono quelle con cui
# il progetto gira davvero oggi, non un minimo teorico — se una va alzata,
# conviene farlo dopo aver eseguito la suite.
#
#   uv venv core/.venv --python 3.12
#   uv pip install --python core/.venv/Scripts/python.exe -r core/requirements.txt

# --- audio: due tracce separate, mai mischiate
# Il loopback WASAPI non c'è in PyAudio normale: serve questa forcella.
PyAudioWPatch==0.2.12.8
sounddevice==0.5.5

# --- trascrizione in locale
# `cpu` evita di tirarsi dietro CUDA, `hub` serve a scaricare il modello.
onnx-asr[cpu,hub]==0.12.0
numpy==2.5.1
scipy==1.18.0

# --- server locale con cui parla l'interfaccia
fastapi==0.140.0
uvicorn[standard]==0.51.0
# Chiamate ai motori di analisi via API e ai connettori di export.
httpx==0.28.1

# --- solo Windows
# OCR di sistema per leggere il testo negli screenshot.
winsdk==1.0.0b10
# Enumerazione delle sessioni audio: è così che si capisce di essere in call
# senza scrivere codice per ogni singolo programma.
pycaw==20251023
comtypes==1.4.16
